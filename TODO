RECORDING:
- Finalize output file format (.cap? .fc?)
- Code a way of discovering pixel type
- Research more optimizations

COMPILING:
- Parse .cap file
- Put frame data into Vec<ImageBuffer> (or other structure)
- Compile Images into video (which format? which library?)

